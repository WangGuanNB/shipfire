# 更新说明：[功能名称]

## 元数据
- **更新日期**：YYYY-MM-DD
- **更新类型**：数据库迁移 / 功能增强 / Bug修复 / 性能优化 / 配置变更
- **影响范围**：数据库层 / API层 / UI层 / 配置文件 / 全栈
- **优先级**：高（安全/性能/破坏性） / 中（功能） / 低（优化）
- **兼容性**：破坏性更新 / 向后兼容
- **预计工作量**：小（<2小时） / 中（2-8小时） / 大（>8小时）

## 更新摘要

[用 2-3 句话简要描述这次更新的内容和目的]

## 背景说明

[可选：解释为什么需要这次更新，解决了什么问题]

## 影响的文件

### 新增文件
- `path/to/new/file1.ts` - 文件说明
- `path/to/new/file2.ts` - 文件说明

### 修改文件
- `path/to/modified/file1.ts` - 修改说明
- `path/to/modified/file2.ts` - 修改说明

### 删除文件
- `path/to/deleted/file1.ts` - 删除原因

## 依赖变更

### 新增依赖
```json
{
  "package-name": "^version"
}
```

### 删除依赖
```json
{
  "old-package": "^version"
}
```

### 版本升级
```json
{
  "package-name": "^old-version" → "^new-version"
}
```

## 配置变更

### 环境变量

#### 新增
```env
NEW_VAR_NAME=value  # 说明
```

#### 修改
```env
# Before
OLD_VAR=old_value

# After
OLD_VAR=new_value
```

#### 删除
```env
# 删除以下变量
DEPRECATED_VAR=xxx
```

### 配置文件

#### wrangler.jsonc
```jsonc
{
  // 新增或修改的配置
}
```

#### next.config.mjs
```javascript
// 新增或修改的配置
```

## 迁移步骤

### 步骤 1：[步骤名称]

**操作：**
```bash
# 执行的命令
```

**说明：**
[详细说明这一步做什么]

### 步骤 2：[步骤名称]

**操作：**
```bash
# 执行的命令
```

**说明：**
[详细说明这一步做什么]

### 步骤 3：[步骤名称]

**操作：**
```bash
# 执行的命令
```

**说明：**
[详细说明这一步做什么]

## 代码示例

### 示例 1：[场景说明]

#### Before (旧代码)
```typescript
// 旧的实现方式
```

#### After (新代码)
```typescript
// 新的实现方式
```

**关键变化：**
- 变化点 1
- 变化点 2

### 示例 2：[场景说明]

#### Before (旧代码)
```typescript
// 旧的实现方式
```

#### After (新代码)
```typescript
// 新的实现方式
```

**关键变化：**
- 变化点 1
- 变化点 2

## 数据迁移（如果需要）

### 导出现有数据
```bash
# 导出命令
```

### 转换数据格式
```bash
# 转换脚本或命令
```

### 导入新数据
```bash
# 导入命令
```

## 注意事项

⚠️ **重要警告**
- 警告事项 1
- 警告事项 2

💡 **最佳实践**
- 建议 1
- 建议 2

✅ **兼容性说明**
- 兼容性说明 1
- 兼容性说明 2

## 验证清单

完成更新后，请逐项检查：

- [ ] 依赖安装成功（`pnpm install`）
- [ ] 环境变量已配置
- [ ] 代码编译无错误（`pnpm build`）
- [ ] 本地开发环境正常（`pnpm dev`）
- [ ] 核心功能测试通过
  - [ ] 功能点 1
  - [ ] 功能点 2
  - [ ] 功能点 3
- [ ] 生产环境部署成功（如果适用）

## 回滚方案

如果更新出现问题，可以按以下步骤回滚：

1. **代码回滚**
   ```bash
   git revert <commit-hash>
   ```

2. **依赖回滚**
   ```bash
   # 恢复 package.json 后
   pnpm install
   ```

3. **数据回滚**（如果有数据迁移）
   ```bash
   # 数据回滚命令
   ```

## 常见问题

### Q1: [问题描述]
**A:** [解决方案]

### Q2: [问题描述]
**A:** [解决方案]

### Q3: [问题描述]
**A:** [解决方案]

## 相关文档

- [相关文档链接 1](url)
- [相关文档链接 2](url)

## 更新日志

- **YYYY-MM-DD**: 初始版本
- **YYYY-MM-DD**: 更新说明（如果有修订）
