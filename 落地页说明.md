# 落地页说明

给配置落地页、以及 Skills / AI 写页面时用的说明：先讲**工具页模块设计意图（Google SEO）**，再讲**JSON 怎么配、组件怎么接**。

> **与 my-page-build-factory 的关系：** Skills 写落地页时会优先读本文件的模块意图；若其他项目没有本文件，则使用 Skills 内置的 `tool-page-module-playbook.md`。

沿用 `src/i18n/pages/landing/en.json`、`zh.json` 和现有主题、组件体系。首页入口只负责读取内容并调用 `LandingRenderer`，不再逐个写死模块。

---

## 一、工具页模块设计意图（Skills 必读）

`page_type: "tool"` 时，按下面思路组织模块。这是「设计意图说明书」：每个模块解决什么用户/搜索问题、大致写什么、SEO 注意什么。

### 1. 总原则（工具落地页）

| 原则 | 说明 |
| --- | --- |
| 搜索意图 | 交易型为主：用户要**马上完成任务**（生成 / 转换 / 计算），不是先读品牌故事 |
| 一页一词 | H1 / Title / 首段对准**一个主关键词**；次要词自然分布 |
| 先做事再解释 | 首屏给操作区；证明与说明跟在能用之后 |
| 诚实 | 登录 / 积分 / 限制写清楚；无真实评价就关 `testimonial` |
| 可索引文字 | 每个模块都要有可读句子，不只靠图或 UI 控件 |
| 字数 | 整页可见文案大约 900–1200 词（英文口径）；密度自然，忌堆砌 |

### 2. 当前默认模块顺序

```text
hero(+tool) → examples → introduce → usage → feature → benefit → testimonial → cost → faq → cta
```

### 3. 逐模块设计思路

#### 3.1 `hero`（首屏 + 工具操作区）— 必选

**设计思路：** 工具站的「任务入口」。用户带着关键词进来，第一件事应是**看见并开始用工具**，而不是先读长介绍。

**要表达什么：**

- H1：完整主关键词 + 结果导向（能做什么）
- 1 句说明：输入是什么、输出是什么、怎么开始
- `metrics`：3 条以内硬条件（如需登录、积分、格式限制）——服务信任，不是口号
- 嵌入 `tool`：真正的操作区（登录态、额度、主 CTA）

**SEO：**

- 页面唯一 H1；主词出现在 H1 + 前 100 字
- Title / Description 与 H1 同意图，不另开第二产品
- 工具区上方仍要有可抓取文案（不只放空白表单）

**Skills 写作提示：** 先写「任务承诺」再写控件文案；禁止首屏只有营销句、没有操作区。

#### 3.2 `examples`（效果 / 结果条）— 强烈建议

**设计思路：** 用**真实结果**降低「这东西行不行」的不确定；并可一键填入提示词，把人送回工具。

**要表达什么：**

- 标题：结果导向（如 Real results / 真实效果），不是空壳 Gallery
- 最多 3 张：输入意图简述 + 输出图 + 可选「填入工具」`action`
- 明确：加载示例 ≠ 扣费 / 开始计费生成

**SEO：**

- 图必须有描述性 `alt`（含场景词，忌 keyword stuffing）
- 提供可索引短说明（每张 1–2 句）
- 证明页价值，间接提升停留与完成任务概率

**Skills 写作提示：** 优先真实出图；示意必须标明；无图则 `disabled`，不要占位糊弄。

#### 3.3 `introduce`（交替图文介绍）— 建议保留

**设计思路：** 不再长篇「What is Brand」。工具页里负责**能力切片 + 输入 / 输出示意**，回答「这工具具体能干哪几类事」。

**要表达什么：**

- 3–4 块：每块一个能力面（如比例、分辨率、流程、边界）
- 图文交替；可链回 `#hero-tool` 或 examples
- 短定义可并进 hero；这里偏「展开」

**SEO：**

- H2 价值导向，避免「什么是 XXX」「核心功能概览」等空标签
- 自然重复主词 / 近义词 1–2 次即可
- 补足 hero 放不下的说明性内容，撑主题深度

**Skills 写作提示：** 每块 = 一个用户任务切面；不要和后面 `feature` 逐条重复。

#### 3.4 `usage`（步骤 How it works）— 建议保留并靠前

**设计思路：** 匹配「怎么用」类附带意图；紧跟 examples，形成「看见结果 → 学会三步」。

**要表达什么：**

- 固定 3 步（偶可 4）：准备输入 → 选参数 / 看成本 → 生成并复核
- 每步一句动作 + 一句结果 / 注意
- 与真实 UI 一致（有登录就写登录，有积分就写先看积分）

**SEO：**

- H2 可含主词变体（如 From X to Y）
- 步骤文案利于 How-to 理解
- 服务信息型附属查询，不抢交易主词

**Skills 写作提示：** 步骤必须可照做；禁止与产品不符的「魔法三步」。

#### 3.5 `feature`（功能网格）— 保留

**设计思路：** 清单化「能做什么 + 使用时要注意什么」；补规格与边界，和 introduce 的叙事块分工。

**要表达什么：**

- 4–6 个短卡片：能力 / 参数 / 限制 / 导出等
- 可含「不支持什么」或「生成后需人工检查」
- 标题避免空泛 Powerful features

**SEO：**

- 覆盖次要词与规格词（格式、比例、语言等）
- 每卡 1–2 句完整句，便于抓取
- 不与 benefit 抢「为什么选我们」话术

**Skills 写作提示：** feature = 能力与边界；benefit = 场景与谁适合。写之前先列互斥清单。

#### 3.6 `benefit`（场景，不是空泛优势）— 保留

**设计思路：** 旧「Why choose us」易与 feature 重复。工具页改为 **Use cases（适用场景）**：谁在什么情况下用。

**要表达什么：**

- 2–4 个场景（角色 + 任务 + 为何用本工具）
- 少品牌形容词，多工作流语言
- 可链回工具或 pricing

**SEO：**

- 吃长尾场景词
- 扩展主题集群，不另开主词
- H2 用场景语义，不用「为什么选择 Brand」

**Skills 写作提示：** 每个场景对应一类可能搜索词；禁止空洞「更快更好更强」。

#### 3.7 `testimonial`（评价）— 可选

**设计思路：** 信任模块；**无真实评价则关闭**。伪造评价伤害信任，SEO 无实质加分。

**要表达什么（仅有真数据时）：**

- 具体结果 / 场景，少空泛五星话术
- 勿虚构用户数量（如 1000+ users）

**SEO：** 几乎不是排名杠杆；假内容风险 > 收益 → Skills 默认 `disabled: true`。

#### 3.8 `cost`（费用 / 额度，`content`）— 工具页建议有

**设计思路：** 合规与预期管理：积分怎么扣、示例是否扣费、不足怎么办。用 `content`，避免再抄一份 pricing 表造成价格双源。

**要表达什么：**

- 计费触发点（点生成才扣 / 预览不扣等）
- 余额展示逻辑（与工具区文案一致）
- 链到 `/pricing/` 看套餐

**SEO：** 回答 is it free / pricing 类附属意图；免费层与付费层说清楚。

**Skills 写作提示：** 必须与 hero metrics、工具 UI 文案一致。

#### 3.9 `faq`（常见问题）— 保留

**设计思路：** 承接异议与长尾问法：登录、版权、质量、积分、商用等。

**要表达什么：**

- 5–7 问；答案短而可执行
- 至少 1 问澄清易误解点（如 demo 与真实产品边界）

**SEO：** 覆盖疑问型长尾；问句自然，勿为塞词硬造问法。FAQ JSON-LD 可选（富结果已弱化）。

#### 3.10 `cta`（收尾）— 轻量保留

**设计思路：** 读完全文后的回流：回工具 / 看定价。不必再开大叙事块。

**要表达什么：**

- 一句行动号召 + 1–2 按钮（`#hero-tool`、`/pricing/`）
- 与首屏承诺一致

**SEO：** 加强内链与转化；锚文本描述性，忌「点击这里」。

### 4. 模块与意图对照表

| 模块 | 用户问题 | SEO 角色 | 默认 |
| --- | --- | --- | --- |
| hero+tool | 怎么马上用？ | 主意图、H1、转化 | 必开 |
| examples | 效果怎样？ | 证明、alt、停留 | 建议开 |
| introduce | 具体能干啥？ | 主题深度、H2 | 建议开 |
| usage | 怎么操作？ | How-to / 附带意图 | 建议开、靠前 |
| feature | 规格与限制？ | 次要词、边界 | 建议开 |
| benefit | 适不适合我？ | 场景长尾 | 建议开（写场景） |
| testimonial | 别人用得怎样？ | 弱信任 | 可选，无真数据则关 |
| cost | 多少钱 / 怎么扣？ | 商业意图澄清 | 工具页建议开 |
| faq | 还有疑虑？ | 长尾问答 | 建议开 |
| cta | 下一步？ | 回流内链 | 建议开、轻量 |

### 5. Skills 禁止事项

1. 把工具页写成纯介绍页（首屏无操作区）
2. feature 与 benefit 写成两套「为什么选我们」
3. 编造评价、用户数、永久免费（若实际要登录 / 积分）
4. 同一主词再做一个几乎同构的路由抢流量
5. H2 用「What is / Core Features / Why Choose {Brand}」等空壳标题
6. 为凑字数重复同一句话

### 6. 一句话总纲

> **工具落地页：首屏完成任务并交代门槛 → 用真实结果证明 → 用介绍 / 步骤 / 功能讲清能力与边界 → 用场景对齐用户 → 用费用与 FAQ 消除疑虑 → 轻量 CTA 回流工具；全程一主词、诚实、可操作。**

---

## 二、两种页面与配置方式

- 当前 landing：`page_type: "tool"`，默认上表模块顺序，使用现有图片生成器作为可替换示例。
- 介绍页完整示例：`src/i18n/pages/landing/presets/introduction/en.json` 和 `zh.json`（卖模板 / 介绍产品时用，与工具页意图不同，勿串台）。
- 独立工具页示例：`/image-generator` 读取 `src/i18n/pages/image-generator/`，同样通过 `LandingRenderer` 渲染。
- 导航栏、页脚继续通过 `header`、`footer` 配置，其 `disabled` 开关照常生效。
- 示例配置文件不会自动增加公开路由。将介绍页示例复制到对应的 landing 文件即可切换首页。
- 如需增加长尾路由，在新路由读取该页的本地化配置，并复用 `LandingRenderer`。页面应提供独立 metadata、canonical 和语言对应链接。

### 控制顺序、增减与开关

```json
{
  "page_type": "tool",
  "modules": [
    { "id": "hero", "type": "hero", "source": "hero", "tool": "primary" },
    { "id": "examples", "type": "examples", "source": "examples" },
    { "id": "usage", "type": "steps", "source": "usage" },
    { "id": "faq", "type": "faq", "source": "faq", "disabled": true }
  ]
}
```

- 数组顺序就是显示顺序。删除一项就不显示该模块；`modules: []` 表示不显示任何主体模块。
- `disabled: true` 可放在模块项或对应内容上，任一关闭即不渲染。
- `surface` 可选：`default` | `alt` | `emphasis`，对应 CSS 变量 `--section-surface*`；不写则按序号自动交替（`cta` 默认 `emphasis`）。
- `source` 先查找 `sections[source]`，再查找旧的顶层字段，例如 `feature`、`usage`。
- 缺少内容时不显示该模块，不会补出默认营销文案。独立 `tool` 模块不要求内容 source。
- `id` 是页面锚点，必须以字母开头，只含字母、数字、下划线或短横线，同页不可重复。
- 同一种组件可重复使用，指定不同 `id` 和 `source` 即可。
- 内容原有的 `name` 在渲染时由模块 `id` 覆盖，以免复用内容时出现重复锚点；配置对象本身不会被修改。
- 不写 `modules` 时，按 `page_type` 使用预设；两者都不写时，保留旧 landing 的模块顺序。
- 开关模块后，也要同步检查自行配置的导航、CTA 链接。介绍页自动目录和案例按钮会自动排除已关闭的目标。

### 可用组件

| type | 内容及用途 |
| --- | --- |
| `hero` | 原有 Hero；可通过 `tool` 嵌入操作区 |
| `tool` | 独立工具插槽，可放在正文任意位置 |
| `intro` | 原有交替图文介绍 |
| `features` | 原有功能网格 |
| `benefits` | 原有优势 / 场景图文轮播，条目数可变 |
| `steps` | 原有步骤组件，条目数可变 |
| `examples` | 标题 + 横向最多 3 张效果图；支持 `action` 一键填入工具 |
| `content` | 段落、小标题、列表、图片和对比表；可用于正文、限制、场景、费用说明 |
| `toc` | 按当前启用模块的标题和顺序生成目录 |
| `related` | 相关工具、文章、资源链接卡片和收尾按钮 |
| `pricing` | 原有实际套餐组件，source 指向完整 Pricing 数据 |
| `testimonials` | 原有评价组件，可选；不再自动假定五星或绑定产品名 |
| `faq` | 原有 FAQ，内容可设 `layout: "stacked"` |
| `cta` | 原有独立收尾 CTA，可选 |
| `branding` / `stats` | 原有品牌标志与数据展示组件，可选 |

目录默认排除 hero、tool、toc、cta；其他模块可设 `include_in_toc: false`。  
`pricing` 需要传入已经按产品规则处理的套餐数据；默认费用说明使用 `content`，避免复制一份结账价格。

### 接入与替换工具

```json
{
  "tools": {
    "primary": {
      "type": "image-generator",
      "config": {
        "promptLabel": "画面描述",
        "buttonText": "生成图片"
      }
    }
  }
}
```

`primary` 只是插槽名，可更名。`type` 对应 `src/components/landing/tools.tsx` 中的注册项。  
新工具只需增加一个适配器，再更改 JSON 的工具类型；页面渲染器不需要了解图片、音频或文本业务。  
未知组件或工具类型会明确报错，避免配置拼写错误后悄悄丢失功能。  
关闭工具可设 `tools.primary.disabled: true`；关闭 Hero 会连同其中的工具一起关闭。  
一个具名工具插槽同页只能挂载一次；多个实例使用不同插槽名。

适配器接收 `{ id, slot, locale, tool }`：`id` 是生成的操作区锚点（如 `hero-tool`），`slot` 是具名插槽。  
注册函数可以异步读取服务器配置，返回实际工具组件。只有启用模块使用到的工具才会加载适配器和其数据依赖。  
图片工具的积分费用仍由现有服务器配置读取，JSON 只负责文案，不控制扣费。

也可以向 `LandingRenderer` 传入 `tools` 和 `renderers`，扩展或覆盖默认工具、展示组件。  
工具放在 Hero 中会使用现有 `tool` 版式；独立 `tool` 模块则允许保留单独的 compact/split 首屏。

### 案例与介绍正文

```json
{
  "sections": {
    "examples": {
      "title": "效果案例",
      "items": [{
        "title": "案例名称",
        "input": { "label": "输入", "text": "实际输入" },
        "output": {
          "label": "输出",
          "image": { "src": "/imgs/your-real-result.webp", "alt": "实际输出说明" }
        },
        "action": {
          "label": "使用此示例",
          "tool": "primary",
          "values": { "prompt": "实际输入", "aspect_ratio": "1:1", "resolution": "2K" }
        }
      }]
    },
    "answer": {
      "title": "问题的答案",
      "paragraphs": ["直接解释。", "补充说明。"],
      "items": [{ "title": "小标题", "description": "正文", "list": ["要点一", "要点二"] }],
      "table": { "columns": ["方案", "适用任务"], "rows": [["A", "任务一"], ["B", "任务二"]] }
    }
  }
}
```

上面的图片路径是写法示例，需要换成真实文件。  
案例按钮仅填入参数并定位操作区，不会直接调用生成接口或消耗积分。  
自定义工具可使用 `useToolSelection(slot)` 接收示例参数；新工具自行定义支持哪些 values。  
正文使用普通文本渲染，支持换行，不接受任意 HTML。

### 评价与旧内容

旧 `introduce`、`benefit`、`branding`、`stats`、`testimonial` 等数据可保留为关闭状态，便于迁移。  
如需重新启用，先替换示例文案，再关闭内容的 `disabled` 标志并加入模块清单。  
评价只有明确提供 `items[].rating` 才显示评分；只有显式配置 `review_schema.product_name` 才输出产品评分结构化数据，均值来自实际提供的评分。

### 验证

```bash
node --import tsx --test tests/landing.test.ts
pnpm exec tsc --noEmit --incremental false
```
